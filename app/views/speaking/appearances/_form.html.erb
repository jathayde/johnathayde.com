<%= form_with(model: speaking_appearance) do |form| %>
  <% if speaking_appearance.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(speaking_appearance.errors.count, "error") %> prohibited this speaking_appearance from being saved:</h2>

      <ul>
        <% speaking_appearance.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
